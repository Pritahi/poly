[build]
builder = "nixpacks"

[deploy]
startCommand = "cp prisma/schema.postgresql.prisma prisma/schema.prisma && npx prisma migrate deploy && npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
